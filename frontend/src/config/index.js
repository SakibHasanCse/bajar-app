const API_URL = 'http://localhost:8000';

const Apis = {
    //product api 
    GetProducts: `${API_URL}/api/product`,
    GetCategories: `${API_URL}/api/category`,
    User: `${API_URL}/api/`
}

export { API_URL, Apis }